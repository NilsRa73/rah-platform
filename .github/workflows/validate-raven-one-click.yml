name: Validate RAH Raven one-click package

on:
  workflow_dispatch:
  push:
    paths:
      - 'INSTALL-RAH-AI-STUDIOS.bat'
      - 'INSTALL-RAH-AI-STUDIOS.ps1'
      - 'UPDATE-RAH-RAVEN.ps1'
      - 'DOBBELTKLIKK-HER-START-RAH-RAVEN.bat'
      - 'START-RAH-RAVEN-V2.bat'
      - 'RAH-RAVEN-*.html'
      - 'RAH-HOME-CONTROL.html'
      - 'RAH-RAVEN-VERSION.json'
      - 'raven-checkpoint-policy.js'
      - 'desktop-bridge/**'
      - 'tests/raven-checkpoint-policy.test.mjs'
      - 'tests/raven-now-v2.test.mjs'
      - 'tests/raven-mission-control.test.mjs'
      - 'tests/raven-project-focus.test.mjs'
      - 'tests/raven-core-demo.test.mjs'
      - 'tests/raven-core-context.test.mjs'
      - '.github/workflows/validate-raven-one-click.yml'
  pull_request:
    paths:
      - 'INSTALL-RAH-AI-STUDIOS.bat'
      - 'INSTALL-RAH-AI-STUDIOS.ps1'
      - 'UPDATE-RAH-RAVEN.ps1'
      - 'DOBBELTKLIKK-HER-START-RAH-RAVEN.bat'
      - 'START-RAH-RAVEN-V2.bat'
      - 'RAH-RAVEN-*.html'
      - 'RAH-HOME-CONTROL.html'
      - 'RAH-RAVEN-VERSION.json'
      - 'raven-checkpoint-policy.js'
      - 'desktop-bridge/**'
      - 'tests/raven-checkpoint-policy.test.mjs'
      - 'tests/raven-now-v2.test.mjs'
      - 'tests/raven-mission-control.test.mjs'
      - 'tests/raven-project-focus.test.mjs'
      - 'tests/raven-core-demo.test.mjs'
      - 'tests/raven-core-context.test.mjs'
      - '.github/workflows/validate-raven-one-click.yml'

permissions:
  contents: read

jobs:
  validate-one-click:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Parse PowerShell scripts
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          foreach ($file in @('INSTALL-RAH-AI-STUDIOS.ps1', 'UPDATE-RAH-RAVEN.ps1')) {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $file),[ref]$tokens,[ref]$errors) | Out-Null
            if ($errors.Count -gt 0) {
              $errors | ForEach-Object { Write-Error "${file}: $($_.Message)" }
              exit 1
            }
            Write-Host "$file syntax: OK"
          }

      - name: Validate manifest and Raven 2.0.17 safety contract
        shell: python
        run: |
          import json
          from pathlib import Path

          manifest = json.loads(Path('RAH-RAVEN-VERSION.json').read_text(encoding='utf-8'))
          assert manifest['product'] == 'RAH Raven'
          assert manifest['version'] == '2.0.17'
          assert manifest['launcher'] == '3.0'

          missing = [p for p in manifest['files'] if not Path(p).is_file()]
          if missing:
              raise SystemExit('Manifest references missing files: ' + ', '.join(missing))

          required = {
              'RAH-RAVEN-START.html','RAH-RAVEN-CORE-DEMO.html','RAH-RAVEN-VISION-CORE.html',
              'RAH-RAVEN-COUNCIL.html','RAH-RAVEN-AGENT-RUNNER.html','RAH-RAVEN-MEMORY-SYNC.html',
              'RAH-RAVEN-MISSION-CONTROL.html','RAH-RAVEN-NOW.html','RAH-RAVEN-NOW-V2.html',
              'RAH-RAVEN-PROJECT.html','index.html','raven-checkpoint-policy.js',
              'desktop-bridge/agent_runner.py','desktop-bridge/raven_bridge.py'
          }
          omitted = sorted(required - set(manifest['files']))
          if omitted:
              raise SystemExit('Updater manifest omits runtime files: ' + ', '.join(omitted))

          p = manifest['privacy']
          assert p['hidden_screen_capture'] is False
          assert p['automatic_sending'] is False
          assert p['memory_sync_requires_explicit_confirmation'] is True
          assert p['mission_completion_requires_explicit_confirmation'] is True
          assert p['raven_now_read_only'] is True
          assert p['project_activation_requires_explicit_confirmation'] is True
          assert p['project_activation_does_not_replace_active_mission'] is True
          assert p['recommended_checkpoint_policy_shared_module'] is True
          assert p['project_mission_relation_shared_policy'] is True
          assert p['raven_context_snapshot_shared'] is True
          assert p['raven_context_snapshot_shared_between_now_and_mission_control'] is True
          assert p['raven_context_snapshot_read_only'] is True
          assert p['raven_context_snapshot_no_storage_writes'] is True
          assert p['raven_context_snapshot_value_based'] is True
          assert p['raven_context_snapshot_includes_active_project'] is True
          assert p['raven_context_snapshot_includes_active_mission'] is True
          assert p['raven_context_snapshot_includes_blocker'] is True
          assert p['raven_context_snapshot_includes_next_step'] is True
          assert p['raven_context_snapshot_includes_project_mission_relation'] is True
          assert p['recommended_checkpoint_uses_context_snapshot'] is True
          assert p['raven_core_context_snapshot_visible'] is True
          assert p['raven_core_uses_shared_context_snapshot'] is True
          assert p['raven_core_context_snapshot_read_only'] is True
          assert p['raven_core_context_snapshot_navigation_only'] is True
          assert p['raven_core_context_snapshot_no_storage_writes'] is True
          assert p['raven_core_recommended_checkpoint_uses_context_snapshot'] is True
          assert p['raven_core_central_continue_visible'] is True
          assert p['raven_core_continue_uses_context_snapshot'] is True
          assert p['raven_core_continue_navigation_only'] is True
          assert p['raven_core_continue_no_storage_writes'] is True
          assert p['raven_core_continue_no_project_switch'] is True
          assert p['raven_core_continue_no_mission_mutation'] is True
          assert p['raven_core_continue_no_step_completion'] is True
          assert p['raven_core_continue_no_agent_run'] is True
          assert p['raven_core_pipeline_status_visible'] is True
          assert p['raven_core_pipeline_status_read_only'] is True
          assert p['raven_core_pipeline_separate_from_operational_direction'] is True
          assert p['raven_core_operational_direction_visible'] is True
          assert p['raven_core_continue_context_snapshot_authoritative'] is True
          assert p['raven_core_pipeline_cannot_control_continue_route'] is True
          assert p['raven_core_next_action_visible'] is True
          assert p['raven_core_next_action_derived_from_context_snapshot'] is True
          assert p['raven_core_next_action_explainer_only'] is True
          assert p['raven_core_next_action_read_only'] is True
          assert p['raven_core_next_action_no_storage_writes'] is True
          assert p['raven_core_next_action_cannot_control_continue_route'] is True
          print('Manifest 2.0.17 / launcher 3.0 / Core next action safety: OK')

      - name: Validate shared Context Snapshot and Core next action boundaries
        shell: python
        run: |
          from pathlib import Path
          import re

          now = Path('RAH-RAVEN-NOW-V2.html').read_text(encoding='utf-8')
          mission = Path('RAH-RAVEN-MISSION-CONTROL.html').read_text(encoding='utf-8')
          core = Path('RAH-RAVEN-CORE-DEMO.html').read_text(encoding='utf-8')
          policy = Path('raven-checkpoint-policy.js').read_text(encoding='utf-8')
          project = Path('RAH-RAVEN-PROJECT.html').read_text(encoding='utf-8')

          assert 'v2.10 · READ ONLY' in now
          assert 'v2.8 · LOCAL FIRST' in mission
          assert 'Raven Core Workflow v1.4' in core
          assert 'Core v1.4 · NEXT ACTION' in core
          assert 'v2.4 · EXPLICIT' in project
          for surface in (now, mission, core, project):
              assert '<script src="raven-checkpoint-policy.js"></script>' in surface
          assert 'const Checkpoint=window.RAHCheckpointPolicy' in now
          assert 'const Checkpoint=window.RAHCheckpointPolicy' in mission
          assert 'const CHECKPOINT = window.RAHCheckpointPolicy' in core
          assert 'const Checkpoint=window.RAHCheckpointPolicy' in project

          assert "Checkpoint.contextSnapshot(s,m,block)" in now
          assert "Checkpoint.recommendSnapshot(snapshot,'now')" in now
          assert 'Checkpoint.contextSnapshot(state,m,blocker)' in mission
          assert 'Checkpoint.recommendSnapshot(snapshot,"mission-control")' in mission
          assert 'CHECKPOINT.contextSnapshot(state,mission,missionBlocker(mission))' in core
          assert 'CHECKPOINT.recommendSnapshot(snapshot,"core")' in core
          assert 'Checkpoint.projectMissionRelation(s,m,index)' in project

          assert 'id="coreDirectionStatus"' in core
          assert 'id="pipelineDirection"' in core
          assert 'id="operationalDirection"' in core
          assert 'OPERATIV RETNING · STYRER FORTSETT' in core
          assert 'id="contextNextActionCard"' in core
          assert 'id="contextNextAction"' in core
          assert 'id="contextNextActionDetail"' in core
          assert 'NESTE HANDLING · INGEN AUTOMATIKK' in core
          assert 'id="coreContinue"' in core
          assert '$("coreContinue").href=checkpoint.href' in core
          assert 'function corePipelineState(' in core
          assert 'function renderCoreDirection(' in core
          assert 'function nextActionHint(' in core

          assert "version:'1.2.0'" in policy
          assert 'function contextSnapshot(' in policy
          assert 'function recommendSnapshot(' in policy
          assert 'function nextMissionStep(' in policy
          assert 'function projectMissionRelation(' in policy
          assert "version:'1.0.0'" in policy
          assert 'localStorage' not in policy
          assert 'document.' not in policy
          assert re.search(r'\bactiveMission\s*=(?!=)', policy) is None
          assert re.search(r'\bactiveProject\s*=(?!=)', policy) is None
          assert re.search(r'\.done\s*=\s*true', policy) is None
          assert '/agent/run' not in policy

          pipeline = core.split('function corePipelineState(',1)[1].split('function renderCoreDirection(',1)[0]
          assert 'localStorage' not in pipeline
          assert 'writeState(' not in pipeline
          assert 'coreContinue' not in pipeline
          assert 'checkpoint.href' not in pipeline
          assert re.search(r'\bactiveMission\s*=(?!=)', pipeline) is None
          assert re.search(r'\bactiveProject\s*=(?!=)', pipeline) is None
          assert re.search(r'\.done\s*=\s*true', pipeline) is None
          assert '/agent/run' not in pipeline

          direction = core.split('function renderCoreDirection(pipeline,checkpoint){',1)[1].split('function nextActionHint(snapshot,checkpoint){',1)[0]
          assert 'pipelineDirection' in direction
          assert 'operationalDirection' in direction
          assert 'coreContinue' not in direction
          assert 'localStorage' not in direction
          assert 'writeState(' not in direction

          next_action = core.split('function nextActionHint(snapshot,checkpoint){',1)[1].split('function renderContextSnapshot(snapshot,checkpoint){',1)[0]
          assert 'snapshot.nextStep' in next_action
          assert 'snapshot.blocker' in next_action
          assert 'coreContinue' not in next_action
          assert 'localStorage' not in next_action
          assert 'writeState(' not in next_action
          assert re.search(r'\bactiveMission\s*=(?!=)', next_action) is None
          assert re.search(r'\bactiveProject\s*=(?!=)', next_action) is None
          assert re.search(r'\.done\s*=\s*true', next_action) is None
          assert '/agent/run' not in next_action
          assert '.href=' not in next_action

          context = core.split('function renderContextSnapshot(snapshot,checkpoint){',1)[1].split('function buildReport(){',1)[0]
          assert 'const nextAction=nextActionHint(snapshot,checkpoint)' in context
          assert 'contextNextAction' in context
          assert 'contextNextActionDetail' in context
          assert '$("coreContinue").href=checkpoint.href' in context
          assert 'nextAction.href' not in context
          assert 'pipeline.href' not in context
          assert 'localStorage.setItem' not in context
          assert 'writeState(' not in context
          assert re.search(r'\bactiveMission\s*=(?!=)', context) is None
          assert re.search(r'\bactiveProject\s*=(?!=)', context) is None
          assert re.search(r'\.done\s*=\s*true', context) is None
          assert '/agent/run' not in context

          assert 'localStorage.setItem(STATE_KEY' not in now
          assert re.search(r'\bs\.activeProject\s*=(?!=)', now) is None
          assert re.search(r'\bstate\.activeProject\s*=(?!=)', now) is None
          assert re.search(r'\bactiveMission\s*=(?!=)', now) is None
          assert 'step.done=true' not in now
          assert '/agent/run' not in now

          assert 'id="activateProject"' in project
          assert 'confirm(' in project
          assert 's.activeProject=index' in project
          assert re.search(r'\bs\.activeMission\s*=(?!=)', project) is None
          assert 'step.done=true' not in project
          assert '/agent/run' not in project
          print('Shared Context Snapshot remains authoritative; Core next-action explainer is read-only and cannot control FORTSETT: OK')

      - name: Validate JavaScript and semantic tests
        shell: powershell
        run: |
          node --check raven-checkpoint-policy.js
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node tests/raven-checkpoint-policy.test.mjs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node tests/raven-now-v2.test.mjs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node tests/raven-mission-control.test.mjs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node tests/raven-project-focus.test.mjs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node tests/raven-core-demo.test.mjs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node tests/raven-core-context.test.mjs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Validate one-click chain
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $installer = Get-Content 'INSTALL-RAH-AI-STUDIOS.ps1' -Raw
          $updater = Get-Content 'UPDATE-RAH-RAVEN.ps1' -Raw
          $bootstrap = Get-Content 'INSTALL-RAH-AI-STUDIOS.bat' -Raw
          $starter = Get-Content 'DOBBELTKLIKK-HER-START-RAH-RAVEN.bat' -Raw
          $launcher = Get-Content 'START-RAH-RAVEN-V2.bat' -Raw

          if ($installer -notmatch 'UPDATE-RAH-RAVEN\.ps1') { throw 'Installer does not call updater.' }
          if ($installer -match '\-NoStart') { throw 'Installer suppresses first automatic Raven start.' }
          if ($updater -notmatch 'START-RAH-RAVEN-V2\.bat') { throw 'Updater does not launch Raven.' }
          if ($bootstrap -notmatch 'INSTALL-RAH-AI-STUDIOS\.ps1') { throw 'Bootstrap does not download installer.' }
          if ($starter -notmatch 'UPDATE-RAH-RAVEN\.ps1') { throw 'Desktop starter does not refresh updater.' }
          if ($launcher -notmatch 'set "RAVEN_URL=%~dp0RAH-RAVEN-NOW-V2\.html"') { throw 'Launcher does not open Raven Now v2.' }
          if ($launcher -notmatch 'set "STUDIO_URL=%~dp0RAH-RAVEN-START\.html"') { throw 'Launcher lost Studio navigation target.' }
          if ($launcher -match 'set "RAVEN_URL=%~dp0RAH-RAVEN-NOW\.html"') { throw 'Launcher reverted to Raven Now v1.' }
          Write-Host 'RAH Raven one-click -> Core next action Raven 2.0.17: OK'
