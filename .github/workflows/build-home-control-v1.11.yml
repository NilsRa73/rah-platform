name: Build RAH Home Control v1.11

on:
  push:
    branches:
      - rah-home-control-v1.11-safe-device-add
    paths:
      - '.github/workflows/build-home-control-v1.11.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rah-home-control-v1.11-safe-device-add
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Build Home Control v1.11
        run: python .github/scripts/build_home_control_v1_11.py
      - name: Validate Home Control v1.11
        run: node tests/rah-home-control.test.mjs
      - name: Commit Home Control v1.11 runtime
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add RAH-HOME-CONTROL.html RAH-HOME-CONTROL-VERSION.json tests/rah-home-control.test.mjs
          git diff --cached --quiet && exit 0
          git commit -m 'Build RAH Home Control v1.11 safe device add'
          git push origin HEAD:rah-home-control-v1.11-safe-device-add
