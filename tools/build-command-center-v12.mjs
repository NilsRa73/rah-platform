import fs from'node:fs';import crypto from'node:crypto';
const read=p=>fs.readFileSync(p,'utf8'),write=(p,s)=>fs.writeFileSync(p,s);function rep(s,a,b,l=a.slice(0,70)){if(!s.includes(a))throw new Error('Missing baseline: '+l);return s.replace(a,b)}function blob(path){const b=fs.readFileSync(path);return crypto.createHash('sha1').update(Buffer.concat([Buffer.from(`blob ${b.length}\0`),b])).digest('hex')}

// Core v1.2
let core=read('rah-command-center-core.js');
core=rep(core,"const CC_VERSION='1.1.0',RAVEN_VERSION='2.0.32',BRIDGE_BASE='http://127.0.0.1:18765',DEVICE_STORAGE_KEY='rah.cc.devices.v1',NODE_AGENT_PORT=18766,NODE_AGENT_PROTOCOL='rah-node-health-v1',NODE_STORAGE_PROTOCOL='rah-node-storage-v1',NODE_ACTIONS_PROTOCOL='rah-node-actions-v2',NODE_LAUNCH_PROTOCOL='rah-node-launch-v1',NODE_HANDOFF_PROTOCOL='rah-node-handoff-v1',ACTION_CHALLENGE_HEADER='X-RAH-Action-Challenge',ACTION_CHALLENGE_TTL_SECONDS=60;","const CC_VERSION='1.2.0',RAVEN_VERSION='2.0.32',BRIDGE_BASE='http://127.0.0.1:18765',DEVICE_STORAGE_KEY='rah.cc.devices.v1',NODE_AGENT_PORT=18766,NODE_AGENT_PROTOCOL='rah-node-health-v2',NODE_STORAGE_PROTOCOL='rah-node-storage-v1',NODE_ACTIONS_PROTOCOL='rah-node-actions-v3',NODE_LAUNCH_PROTOCOL='rah-node-launch-v1',NODE_HANDOFF_PROTOCOL='rah-node-handoff-v1',ACTION_CHALLENGE_HEADER='X-RAH-Action-Challenge',ACTION_CHALLENGE_TTL_SECONDS=60;",'core constants');
core=rep(core,"function sanitizeCapabilities(v){if(!Array.isArray(v))return[];const out=[];for(const raw of v){const id=cleanText(raw,'',32).toLowerCase();if(CAPABILITY_IDS.includes(id)&&!out.includes(id))out.push(id)}return out}\nfunction sanitizePermissions", "function sanitizeCapabilities(v){if(!Array.isArray(v))return[];const out=[];for(const raw of v){const id=cleanText(raw,'',32).toLowerCase();if(CAPABILITY_IDS.includes(id)&&!out.includes(id))out.push(id)}return out}\nfunction sanitizeSessionId(v){return typeof v==='string'&&v===v.trim()&&/^[A-Za-z0-9_-]{20,64}$/.test(v)?v:''}\nfunction sanitizePermissions",'session sanitizer');
core=rep(core,"function sanitizeNodeHealth(p){if(!isPlainObject(p)||p.protocol!==NODE_AGENT_PROTOCOL||p.status!=='ready')return null;const capabilities=sanitizeCapabilities(p.capabilities);return{protocol:NODE_AGENT_PROTOCOL,status:'ready',agentVersion:cleanText(p.agentVersion,'unknown',24),hostname:cleanText(p.hostname,'Unknown host',80),platform:cleanText(p.platform,'Unknown platform',80),platformRelease:cleanText(p.platformRelease,'',80),machine:cleanText(p.machine,'',40),nodeName:cleanText(p.nodeName,'',80),nodeRole:cleanText(p.nodeRole,'',100),capabilities,permissions:sanitizePermissions(p.permissions,capabilities)}}","function sanitizeNodeHealth(p){if(!isPlainObject(p)||p.protocol!==NODE_AGENT_PROTOCOL||p.status!=='ready')return null;const sessionId=sanitizeSessionId(p.sessionId);if(!sessionId)return null;const capabilities=sanitizeCapabilities(p.capabilities);return{protocol:NODE_AGENT_PROTOCOL,status:'ready',sessionId,agentVersion:cleanText(p.agentVersion,'unknown',24),hostname:cleanText(p.hostname,'Unknown host',80),platform:cleanText(p.platform,'Unknown platform',80),platformRelease:cleanText(p.platformRelease,'',80),machine:cleanText(p.machine,'',40),nodeName:cleanText(p.nodeName,'',80),nodeRole:cleanText(p.nodeRole,'',100),capabilities,permissions:sanitizePermissions(p.permissions,capabilities)}}",'health session');
core=rep(core,"function sanitizeActionCatalog(p,capabilities){if(!isPlainObject(p)||p.protocol!==NODE_ACTIONS_PROTOCOL||p.status!=='ready'||!Array.isArray(p.actions))return null;const caps=sanitizeCapabilities(capabilities),out=[];for(const raw of p.actions){const row=validateActionRow(raw,caps);if(row&&!out.includes(row.id))out.push(row.id)}return{protocol:NODE_ACTIONS_PROTOCOL,status:'ready',actions:out}}\nfunction actionChallengeFromCatalog(p,capabilities,actionId){if(!isPlainObject(p)||p.protocol!==NODE_ACTIONS_PROTOCOL||p.status!=='ready'||!Array.isArray(p.actions))return'';const caps=sanitizeCapabilities(capabilities),wanted=cleanText(actionId,'',48);for(const raw of p.actions){const row=validateActionRow(raw,caps);if(row&&row.id===wanted)return row.challenge}return''}","function sanitizeActionCatalog(p,capabilities,expectedSessionId){if(!isPlainObject(p)||p.protocol!==NODE_ACTIONS_PROTOCOL||p.status!=='ready'||!Array.isArray(p.actions))return null;const sessionId=sanitizeSessionId(p.sessionId),expected=sanitizeSessionId(expectedSessionId);if(!sessionId||(expected&&sessionId!==expected))return null;const caps=sanitizeCapabilities(capabilities),out=[];for(const raw of p.actions){const row=validateActionRow(raw,caps);if(row&&!out.includes(row.id))out.push(row.id)}return{protocol:NODE_ACTIONS_PROTOCOL,status:'ready',sessionId,actions:out}}\nfunction actionChallengeFromCatalog(p,capabilities,actionId,expectedSessionId){const catalog=sanitizeActionCatalog(p,capabilities,expectedSessionId);if(!catalog)return'';const caps=sanitizeCapabilities(capabilities),wanted=cleanText(actionId,'',48);for(const raw of p.actions){const row=validateActionRow(raw,caps);if(row&&row.id===wanted)return row.challenge}return''}",'catalog session');
const oldNorm="function normalizeDeviceRecord(v,i){if(!isPlainObject(v))return null;const f='device-'+String((i||0)+1),kind=DEVICE_KINDS.includes(v.kind)?v.kind:'other',status=DEVICE_STATUSES.includes(v.status)?v.status:'unverified',endpointIp=normalizeNodeIpv4(v.endpointIp||''),enrolled=v.enrolled===true&&!!endpointIp,capabilities=enrolled?sanitizeCapabilities(v.capabilities):[],advertisedActions=enrolled?sanitizeActionIds(v.advertisedActions):[],approvedRequested=enrolled?sanitizeActionIds(v.approvedActions):[],approvedActions=approvedRequested.filter(id=>advertisedActions.includes(id));return{id:cleanDeviceId(v.id,f),label:cleanText(v.label,'Unnamed device',80),role:cleanText(v.role,'Unassigned role',100),platform:cleanText(v.platform,'Unknown platform',80),kind,status,source:v.source==='seed'?'seed':'local',enrolled,endpointIp:enrolled?endpointIp:'',agentHostname:enrolled?cleanText(v.agentHostname,'',80):'',agentVersion:enrolled?cleanText(v.agentVersion,'',24):'',agentProtocol:enrolled?NODE_AGENT_PROTOCOL:'',capabilities,permissions:sanitizePermissions(v.permissions,capabilities),advertisedActions,approvedActions,remoteControlEnabled:false,commandsEnabled:false}}";
const newNorm="function normalizeDeviceRecord(v,i){if(!isPlainObject(v))return null;const f='device-'+String((i||0)+1),kind=DEVICE_KINDS.includes(v.kind)?v.kind:'other',status=DEVICE_STATUSES.includes(v.status)?v.status:'unverified',endpointIp=normalizeNodeIpv4(v.endpointIp||''),sessionId=sanitizeSessionId(v.agentSessionId||''),enrolled=v.enrolled===true&&!!endpointIp&&!!sessionId,capabilities=enrolled?sanitizeCapabilities(v.capabilities):[],advertisedActions=enrolled?sanitizeActionIds(v.advertisedActions):[],approvedRequested=enrolled?sanitizeActionIds(v.approvedActions):[],approvedActions=approvedRequested.filter(id=>advertisedActions.includes(id));return{id:cleanDeviceId(v.id,f),label:cleanText(v.label,'Unnamed device',80),role:cleanText(v.role,'Unassigned role',100),platform:cleanText(v.platform,'Unknown platform',80),kind,status,source:v.source==='seed'?'seed':'local',enrolled,endpointIp:enrolled?endpointIp:'',agentHostname:enrolled?cleanText(v.agentHostname,'',80):'',agentVersion:enrolled?cleanText(v.agentVersion,'',24):'',agentProtocol:enrolled?NODE_AGENT_PROTOCOL:'',agentSessionId:enrolled?sessionId:'',capabilities,permissions:sanitizePermissions(v.permissions,capabilities),advertisedActions,approvedActions,remoteControlEnabled:false,commandsEnabled:false}}";core=rep(core,oldNorm,newNorm,'device session');
const oldEnroll="function enrollDevice(records,id,ip,healthPayload,actionsPayload){const n=normalizeDeviceRegistry(records),t=cleanDeviceId(id,''),e=normalizeNodeIpv4(ip),h=sanitizeNodeHealth(healthPayload);if(!t||!e||!h)return n;const a=sanitizeActionCatalog(actionsPayload,h.capabilities);if(!a)return n;return n.map(x=>x.id!==t?x:{...x,enrolled:true,endpointIp:e,agentHostname:h.hostname,agentVersion:h.agentVersion,agentProtocol:NODE_AGENT_PROTOCOL,platform:h.platformRelease?(h.platform+' '+h.platformRelease).slice(0,80):h.platform,capabilities:h.capabilities,permissions:h.permissions,advertisedActions:a.actions,approvedActions:sanitizeActionIds(x.approvedActions).filter(actionId=>a.actions.includes(actionId)),remoteControlEnabled:false,commandsEnabled:false})}\nfunction forgetEnrollment(records,id){const n=normalizeDeviceRegistry(records),t=cleanDeviceId(id,'');return n.map(x=>x.id!==t?x:{...x,enrolled:false,endpointIp:'',agentHostname:'',agentVersion:'',agentProtocol:'',capabilities:[],permissions:sanitizePermissions(null,[]),advertisedActions:[],approvedActions:[],remoteControlEnabled:false,commandsEnabled:false})}";
const newEnroll="function normalizeVerifiedCatalog(v,capabilities){if(!isPlainObject(v)||v.protocol!==NODE_ACTIONS_PROTOCOL||v.status!=='ready')return null;const sessionId=sanitizeSessionId(v.sessionId),caps=sanitizeCapabilities(capabilities),actions=sanitizeActionIds(v.actions).filter(id=>caps.includes(ACTION_CATALOG[id].capability));return sessionId?{protocol:NODE_ACTIONS_PROTOCOL,status:'ready',sessionId,actions}:null}\nfunction enrollDevice(records,id,ip,healthPayload,verifiedCatalog){const n=normalizeDeviceRegistry(records),t=cleanDeviceId(id,''),e=normalizeNodeIpv4(ip),h=sanitizeNodeHealth(healthPayload),a=normalizeVerifiedCatalog(verifiedCatalog,h?h.capabilities:[]);if(!t||!e||!h||!a||a.sessionId!==h.sessionId)return n;return n.map(x=>{if(x.id!==t)return x;const sameSession=x.agentSessionId===h.sessionId;return{...x,enrolled:true,endpointIp:e,agentHostname:h.hostname,agentVersion:h.agentVersion,agentProtocol:NODE_AGENT_PROTOCOL,agentSessionId:h.sessionId,platform:h.platformRelease?(h.platform+' '+h.platformRelease).slice(0,80):h.platform,capabilities:h.capabilities,permissions:h.permissions,advertisedActions:a.actions,approvedActions:sameSession?sanitizeActionIds(x.approvedActions).filter(actionId=>a.actions.includes(actionId)):[],remoteControlEnabled:false,commandsEnabled:false}})}\nfunction forgetEnrollment(records,id){const n=normalizeDeviceRegistry(records),t=cleanDeviceId(id,'');return n.map(x=>x.id!==t?x:{...x,enrolled:false,endpointIp:'',agentHostname:'',agentVersion:'',agentProtocol:'',agentSessionId:'',capabilities:[],permissions:sanitizePermissions(null,[]),advertisedActions:[],approvedActions:[],remoteControlEnabled:false,commandsEnabled:false})}";core=rep(core,oldEnroll,newEnroll,'enrollment session');
core=rep(core,"return{CC_VERSION,RAVEN_VERSION,BRIDGE_BASE,DEVICE_STORAGE_KEY,NODE_AGENT_PORT,NODE_AGENT_PROTOCOL,NODE_STORAGE_PROTOCOL,NODE_ACTIONS_PROTOCOL,NODE_LAUNCH_PROTOCOL,NODE_HANDOFF_PROTOCOL,ACTION_CHALLENGE_HEADER,ACTION_CHALLENGE_TTL_SECONDS,CAPABILITY_IDS,ACTION_IDS,ACTION_CATALOG,READ_ONLY_PERMISSIONS,FALLBACK_STABLE_COMPONENTS,COMPONENT_META,PACKAGE_COMPONENTS,DEFAULT_DEVICES,DEVICE_KINDS,DEVICE_STATUSES,isSafeRelativeEntry,normalizeStableComponents,buildCoreSnapshot,parseIpv4,isAllowedNodeIpv4,normalizeNodeIpv4,nodeHealthUrl,nodeActionsUrl,nodeStorageUrl,nodeRustDeskLaunchUrl,nodeRustDeskHandoffUrl,sanitizeCapabilities,sanitizePermissions,sanitizeNodeHealth,sanitizeActionIds,sanitizeActionChallenge,sanitizeActionCatalog,actionChallengeFromCatalog", "return{CC_VERSION,RAVEN_VERSION,BRIDGE_BASE,DEVICE_STORAGE_KEY,NODE_AGENT_PORT,NODE_AGENT_PROTOCOL,NODE_STORAGE_PROTOCOL,NODE_ACTIONS_PROTOCOL,NODE_LAUNCH_PROTOCOL,NODE_HANDOFF_PROTOCOL,ACTION_CHALLENGE_HEADER,ACTION_CHALLENGE_TTL_SECONDS,CAPABILITY_IDS,ACTION_IDS,ACTION_CATALOG,READ_ONLY_PERMISSIONS,FALLBACK_STABLE_COMPONENTS,COMPONENT_META,PACKAGE_COMPONENTS,DEFAULT_DEVICES,DEVICE_KINDS,DEVICE_STATUSES,isSafeRelativeEntry,normalizeStableComponents,buildCoreSnapshot,parseIpv4,isAllowedNodeIpv4,normalizeNodeIpv4,nodeHealthUrl,nodeActionsUrl,nodeStorageUrl,nodeRustDeskLaunchUrl,nodeRustDeskHandoffUrl,sanitizeCapabilities,sanitizeSessionId,sanitizePermissions,sanitizeNodeHealth,sanitizeActionIds,sanitizeActionChallenge,sanitizeActionCatalog,actionChallengeFromCatalog",'core export session');
core=rep(core,'sanitizeHandoffResult,normalizeDeviceRecord,normalizeDeviceRegistry','sanitizeHandoffResult,normalizeDeviceRecord,normalizeDeviceRegistry,normalizeVerifiedCatalog','core export verified catalog');write('rah-command-center-core.js',core);

// Node Agent v0.8 session-bound identity
let agent=read('rah-node-agent.py');
agent=rep(agent,'AGENT_VERSION="0.7.0";PROTOCOL="rah-node-health-v1";STORAGE_PROTOCOL="rah-node-storage-v1";ACTIONS_PROTOCOL="rah-node-actions-v2";LAUNCH_PROTOCOL="rah-node-launch-v1";HANDOFF_PROTOCOL="rah-node-handoff-v1";ACTION_CHALLENGE_HEADER="X-RAH-Action-Challenge";ACTION_CHALLENGE_TTL_SECONDS=60;PORT=18766','AGENT_VERSION="0.8.0";PROTOCOL="rah-node-health-v2";STORAGE_PROTOCOL="rah-node-storage-v1";ACTIONS_PROTOCOL="rah-node-actions-v3";LAUNCH_PROTOCOL="rah-node-launch-v1";HANDOFF_PROTOCOL="rah-node-handoff-v1";ACTION_CHALLENGE_HEADER="X-RAH-Action-Challenge";ACTION_CHALLENGE_TTL_SECONDS=60;PORT=18766','agent constants');
agent=rep(agent,'def sanitize_capabilities(values):','def sanitize_session_id(value):\n value=str(value or "")\n return value if re.fullmatch(r"[A-Za-z0-9_-]{20,64}",value) else ""\ndef sanitize_capabilities(values):','agent session sanitizer');
agent=rep(agent,'def build_health_payload(node_name="",node_role="",capabilities=None):\n caps=sanitize_capabilities(capabilities)\n return {"protocol":PROTOCOL,"agentVersion":AGENT_VERSION,"status":"ready","hostname":clean_text(socket.gethostname(),80) or "Unknown host","platform":clean_text(platform.system(),80) or "Unknown platform","platformRelease":clean_text(platform.release(),80),"machine":clean_text(platform.machine(),40),"nodeName":clean_text(node_name,80),"nodeRole":clean_text(node_role,100),"capabilities":caps,"permissions":build_permissions(caps)}','def build_health_payload(node_name="",node_role="",capabilities=None,session_id=""):\n caps=sanitize_capabilities(capabilities);session=sanitize_session_id(session_id)\n return {"protocol":PROTOCOL,"agentVersion":AGENT_VERSION,"status":"ready","sessionId":session,"hostname":clean_text(socket.gethostname(),80) or "Unknown host","platform":clean_text(platform.system(),80) or "Unknown platform","platformRelease":clean_text(platform.release(),80),"machine":clean_text(platform.machine(),40),"nodeName":clean_text(node_name,80),"nodeRole":clean_text(node_role,100),"capabilities":caps,"permissions":build_permissions(caps)}','agent health payload');
agent=rep(agent,'def build_actions_payload(capabilities=None,app_paths=None):\n caps=sanitize_capabilities(capabilities);paths=build_app_paths(app_paths);actions=[]','def build_actions_payload(capabilities=None,app_paths=None,session_id=""):\n caps=sanitize_capabilities(capabilities);paths=build_app_paths(app_paths);session=sanitize_session_id(session_id);actions=[]','agent actions signature');
agent=rep(agent,' return {"protocol":ACTIONS_PROTOCOL,"status":"ready","actions":actions,"approvalMode":"command-center-local"}\ndef issue_action_challenges',' return {"protocol":ACTIONS_PROTOCOL,"status":"ready","sessionId":session,"actions":actions,"approvalMode":"command-center-local"}\ndef issue_action_challenges','agent actions session');
agent=rep(agent,' return {"protocol":ACTIONS_PROTOCOL,"status":"ready","actions":actions,"approvalMode":"command-center-local"}\ndef consume_action_challenge',' return {"protocol":ACTIONS_PROTOCOL,"status":"ready","sessionId":base_payload.get("sessionId",""),"actions":actions,"approvalMode":"command-center-local"}\ndef consume_action_challenge','challenge preserve session');
agent=rep(agent,'def make_handler(token,node_name="",node_role="",capabilities=None,app_paths=None,app_launcher=None,handoff_launcher=None,challenge_ttl_seconds=ACTION_CHALLENGE_TTL_SECONDS):\n caps=sanitize_capabilities(capabilities);paths=build_app_paths(app_paths);health_payload=build_health_payload(node_name,node_role,caps);actions_payload=build_actions_payload(caps,paths);challenge_state={};challenge_lock=threading.Lock();launcher=app_launcher or launch_executable;handoff=handoff_launcher or launch_rustdesk_connect','def make_handler(token,node_name="",node_role="",capabilities=None,app_paths=None,app_launcher=None,handoff_launcher=None,challenge_ttl_seconds=ACTION_CHALLENGE_TTL_SECONDS,session_id=None):\n caps=sanitize_capabilities(capabilities);paths=build_app_paths(app_paths);session=sanitize_session_id(session_id) or secrets.token_urlsafe(18);health_payload=build_health_payload(node_name,node_role,caps,session);actions_payload=build_actions_payload(caps,paths,session);challenge_state={};challenge_lock=threading.Lock();launcher=app_launcher or launch_executable;handoff=handoff_launcher or launch_rustdesk_connect','handler session');
agent=rep(agent,'  server_version="RAHNodeAgent/0.6";sys_version=""','  server_version="RAHNodeAgent/0.8";sys_version=""','server version sync');
agent=rep(agent,'def create_server(host,port,token,node_name="",node_role="",capabilities=None,app_paths=None,app_launcher=None,handoff_launcher=None,challenge_ttl_seconds=ACTION_CHALLENGE_TTL_SECONDS):return ThreadingHTTPServer((host,port),make_handler(token,node_name,node_role,capabilities,app_paths,app_launcher,handoff_launcher,challenge_ttl_seconds))','def create_server(host,port,token,node_name="",node_role="",capabilities=None,app_paths=None,app_launcher=None,handoff_launcher=None,challenge_ttl_seconds=ACTION_CHALLENGE_TTL_SECONDS,session_id=None):return ThreadingHTTPServer((host,port),make_handler(token,node_name,node_role,capabilities,app_paths,app_launcher,handoff_launcher,challenge_ttl_seconds,session_id))','server session');
agent=rep(agent,'p=argparse.ArgumentParser(description="RAH Node Agent — explicit identity, fixed action catalog, one-time action challenges, read-only storage, fixed app launch and password-free RustDesk handoff")','p=argparse.ArgumentParser(description="RAH Node Agent — session-bound identity, fixed action catalog, one-time action challenges, read-only storage, fixed app launch and password-free RustDesk handoff")','description');
agent=rep(agent,'server=create_server(host,PORT,token,args.name,args.role,capabilities,paths);actions=build_actions_payload(capabilities,paths)["actions"]','server=create_server(host,PORT,token,args.name,args.role,capabilities,paths);session=server.RequestHandlerClass.__closure__;actions=build_actions_payload(capabilities,paths)["actions"]','main baseline temp');
// Replace the temporary closure line with simple startup messaging; session stays server-internal and is visible through authenticated /health.
agent=agent.replace('session=server.RequestHandlerClass.__closure__;actions=build_actions_payload(capabilities,paths)["actions"]','actions=build_actions_payload(capabilities,paths)["actions"]');
agent=rep(agent,'print("GET /health and GET /actions are authenticated. Each explicit /actions read issues fresh 60-second single-use action challenges in memory only.")','print("GET /health and GET /actions are authenticated and carry the same fresh Node Agent session ID for this process. Agent restart rotates both token and session ID.");print("Each explicit /actions read issues fresh 60-second single-use action challenges in memory only.")','agent messaging');write('rah-node-agent.py',agent);

// HTML v1.2 + enrollment bugfix + session checks
let html=read('RAH-COMMAND-CENTER-V1.1.html');
html=html.replaceAll('v1.1.0','v1.2.0').replaceAll('v1.1 ·','v1.2 ·').replaceAll('CC v1.1.0','CC v1.2.0');
html=rep(html,'RAH AI STUDIOS · ONE-TIME ACTION CHALLENGE','RAH AI STUDIOS · SESSION-BOUND ENROLLMENT','html eyebrow');
html=rep(html,'Every approved action now requires a fresh single-use Node Agent challenge. No new action power, native remote-control endpoint, shell or arbitrary arguments.','Enrollment is now bound to one Node Agent process session. Agent restart forces explicit re-verification before actions can run; no new action power is added.','html intro');
html=rep(html,'Two authenticated GETs after one explicit click. Catalog challenges are transient and never enrolled or saved.','Two authenticated GETs must report the same fresh agent session. Catalog challenges are discarded immediately after sanitization.','node check subtitle');
html=rep(html,'The action list is authoritative; one-time challenges returned during enrollment are deliberately discarded and never saved.','The health and action responses must carry the same session ID. Only the sanitized session ID + fixed action IDs are retained for the pending enrollment; challenge values are discarded.','node check note');
html=rep(html,'Nothing approved automatically. Approval metadata contains no bearer token, peer ID or password.','Nothing approved automatically. A new agent session clears prior approvals on re-enrollment; approval metadata contains no bearer token, challenge, peer ID or password.','policy subtitle');
html=rep(html,'Saved registry excludes bearer tokens, action challenges, handoff target IDs and action results.','Saved registry contains the non-secret enrolled agent session ID, but excludes bearer tokens, action challenges, handoff target IDs and action results.','devices subtitle');
html=rep(html,'RAH Raven Command Center v1.2 · one-time action challenge · no challenge/token/peer-ID/password storage · no arbitrary process · no shell · no installer · no native remote-control endpoint.','RAH Raven Command Center v1.2 · session-bound enrollment + one-time challenge · no challenge/token/peer-ID/password storage · no arbitrary process · no shell · no installer · no native remote-control endpoint.','footer');
html=rep(html,"el.querySelector('.device-meta').innerHTML='<span>Platform: '+d.platform+'</span><span>Endpoint: '+(d.endpointIp||'not enrolled')+'</span><span>Agent: '+(d.agentVersion||'—')+'</span>';","el.querySelector('.device-meta').innerHTML='<span>Platform: '+d.platform+'</span><span>Endpoint: '+(d.endpointIp||'not enrolled')+'</span><span>Agent: '+(d.agentVersion||'—')+'</span><span>Session: '+(d.agentSessionId?d.agentSessionId.slice(0,8)+'…':'—')+'</span>';",'device session display');
const oldCheck="const catalog=core.sanitizeActionCatalog(actionRes.payload,health.capabilities);if(!catalog)throw new Error('Invalid action catalog.');pendingEnrollment={id,ip,health,catalog};checks.set(id,true);out.className='result ok';out.textContent='ONLINE NOW · '+health.hostname+' · capabilities: '+(health.capabilities.join(', ')||'identity-only')+' · actions: '+(catalog.actions.join(', ')||'none');";
const newCheck="const catalog=core.sanitizeActionCatalog(actionRes.payload,health.capabilities,health.sessionId);if(!catalog)throw new Error('Invalid or cross-session action catalog.');pendingEnrollment={id,ip,health,catalog};checks.set(id,true);out.className='result ok';out.textContent='ONLINE NOW · '+health.hostname+' · session '+health.sessionId.slice(0,8)+'… · capabilities: '+(health.capabilities.join(', ')||'identity-only')+' · actions: '+(catalog.actions.join(', ')||'none');";html=rep(html,oldCheck,newCheck,'check node session');
const oldEnrollUi="devices=core.enrollDevice(devices,pendingEnrollment.id,pendingEnrollment.ip,pendingEnrollment.health,{protocol:core.NODE_ACTIONS_PROTOCOL,status:'ready',actions:pendingEnrollment.catalog.actions.map(id=>core.ACTION_CATALOG[id])});saveDevices();out.className='result ok';out.textContent='Verified enrollment saved without token.';pendingEnrollment=null;";
const newEnrollUi="const expectedSession=pendingEnrollment.health.sessionId;devices=core.enrollDevice(devices,pendingEnrollment.id,pendingEnrollment.ip,pendingEnrollment.health,pendingEnrollment.catalog);const saved=devices.find(d=>d.id===pendingEnrollment.id);if(!saved||!saved.enrolled||saved.agentSessionId!==expectedSession){out.className='result bad';out.textContent='Enrollment was rejected by the session-bound core.';return}saveDevices();out.className='result ok';out.textContent='Verified session-bound enrollment saved without token or challenge. Approvals are cleared if the agent session changed.';pendingEnrollment=null;";html=rep(html,oldEnrollUi,newEnrollUi,'enrollment no-op fix');
const oldFresh="const catalog=core.sanitizeActionCatalog(res.payload,d.capabilities),challenge=core.actionChallengeFromCatalog(res.payload,d.capabilities,action);if(!catalog||!catalog.actions.includes(action)||!challenge)throw new Error('Node did not return a valid fresh challenge for this approved action.');return challenge";
const newFresh="const catalog=core.sanitizeActionCatalog(res.payload,d.capabilities,d.agentSessionId),challenge=core.actionChallengeFromCatalog(res.payload,d.capabilities,action,d.agentSessionId);if(!catalog)throw new Error('Node Agent session changed. Re-check and re-enroll this node before running actions.');if(!catalog.actions.includes(action)||!challenge)throw new Error('Node did not return a valid fresh challenge for this approved action.');return challenge";html=rep(html,oldFresh,newFresh,'fresh action session');
write('RAH-COMMAND-CENTER-V1.2.html',html);

// Manifest candidate
const m=JSON.parse(read('RAH-COMMAND-CENTER-VERSION.json'));m.version='1.2.0';m.stage='feature-candidate';delete m.stable_since;m.entry='RAH-COMMAND-CENTER-V1.2.html';m.previous_stable_version='1.1.0';m.package_files[0]='RAH-COMMAND-CENTER-V1.2.html';Object.assign(m.features,{node_agent_action_catalog_protocol:'rah-node-actions-v3',node_agent_health_protocol:'rah-node-health-v2',session_bound_enrollment:true,agent_session_id_rotates_on_start:true,agent_session_id_random_source:'secrets.token_urlsafe-18-bytes',agent_session_id_node_memory_only:true,agent_session_id_secret:false,enrollment_session_id_persistence:true,enrollment_session_id_non_secret:true,enrollment_health_actions_session_match_required:true,enrollment_verified_catalog_sanitized_only:true,enrollment_pending_raw_action_challenge:false,enrollment_raw_action_challenge_persistence:false,agent_session_mismatch_blocks_action_challenge:true,agent_session_change_requires_reverification:true,agent_session_change_clears_approvals_on_reenroll:true});m.node_agent.protocol='rah-node-health-v2';m.node_agent.actions_protocol='rah-node-actions-v3';m.node_agent.agent_version='0.8.0';m.node_agent.session={id_source:'secrets.token_urlsafe-18-bytes',rotates_on_start:true,node_memory_only:true,secret:false,health_actions_match_required:true,enrollment_persists_id:true,mismatch_blocks_actions:true,reenrollment_clears_approvals_on_change:true};m.release_gate={status:'candidate',gate_version:'1.8.0-candidate',requires_tests:['tests/rah-command-center-core.test.mjs','tests/rah-command-center-v12.test.mjs','tests/rah-command-center-packaging.test.mjs','tests/test_rah_node_agent.py'],stable_raven_runtime_frozen:true,runtime_files_frozen:false,change_policy:'session-bound-enrollment-only'};m.runtime_feature_change=true;m.development_reopened=true;m.development_paused=false;m.change_policy='session-bound-enrollment-only';write('RAH-COMMAND-CENTER-VERSION.json',JSON.stringify(m,null,2)+'\n');

// Launchers/updater
let launch=read('DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat');launch=launch.replaceAll('V1.1','V1.2').replaceAll('v1.1.0','v1.2.0');write('DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat',launch);let up=read('UPDATE-RAH-COMMAND-CENTER.ps1');up=rep(up,'RAH-COMMAND-CENTER-V1.1.html','RAH-COMMAND-CENTER-V1.2.html','updater entry');up=up.replace('Alle actions krever remote/capability-regler, annonsert allowlist-action, lokal godkjenning, ferskt token og en fersk single-use action challenge; challenge/token/peerId/password lagres ikke, og frie paths/argumenter/server/key/URL, installasjon, shell, filer, generiske kommandoer og native fjernstyring er av.','Alle actions krever annonsert action, capability, lokal godkjenning, ferskt token, matchende enrolled agent-session og en fersk single-use challenge; agent-restart krever ny verifisering, og challenge/token/peerId/password lagres ikke. Frie paths/argumenter/server/key/URL, installasjon, shell, filer, generiske kommandoer og native fjernstyring er av.');write('UPDATE-RAH-COMMAND-CENTER.ps1',up);

// Historical v1.1 artifact test
const hist=String.raw`import assert from'node:assert/strict';import fs from'node:fs';import crypto from'node:crypto';const path='RAH-COMMAND-CENTER-V1.1.html',html=fs.readFileSync(path,'utf8'),b=fs.readFileSync(path),sha=crypto.createHash('sha1').update(Buffer.concat([Buffer.from('blob '+b.length+'\0'),b])).digest('hex');assert.equal(sha,'980121398c73c76a7fb54e9568ef02dfe7ad0f24','v1.1 Stable HTML moved');assert.match(html,/CC v1\.1\.0/);assert.match(html,/ONE-TIME ACTION CHALLENGE/);assert.doesNotMatch(html,/id="handoffPassword"|--password/);console.log('RAH Command Center v1.1 historical artifact preserved');`;write('tests/rah-command-center-v11.test.mjs',hist+'\n');

// Current core tests
const coreTest=String.raw`import assert from'node:assert/strict';import{createRequire}from'node:module';const require=createRequire(import.meta.url),core=require('../rah-command-center-core.js');const s1='SessionId_abcdefghijklmnop',s2='SessionId_qrstuvwxyz123456',ch='Challenge_abcdefghijklmnopqrstu';assert.equal(core.CC_VERSION,'1.2.0');assert.equal(core.NODE_AGENT_PROTOCOL,'rah-node-health-v2');assert.equal(core.NODE_ACTIONS_PROTOCOL,'rah-node-actions-v3');assert.equal(core.sanitizeSessionId(s1),s1);for(const bad of['','short','bad session','../../bad_session_identifier'])assert.equal(core.sanitizeSessionId(bad),'');const health=core.sanitizeNodeHealth({protocol:'rah-node-health-v2',status:'ready',sessionId:s1,agentVersion:'0.8.0',hostname:'NODE',platform:'Linux',platformRelease:'6.8',capabilities:['storage','remote-desktop','shell'],permissions:{commands:true,shell:true,remoteControl:true}});assert.equal(health.sessionId,s1);assert.deepEqual(health.capabilities,['storage','remote-desktop']);assert.equal(health.permissions.shell,false);const rows=[{...core.ACTION_CATALOG['storage-summary.read'],challenge:ch,challengeTtlSeconds:60},{...core.ACTION_CATALOG['rustdesk.launch'],challenge:'Challenge_qrstuvwxyz1234567890',challengeTtlSeconds:60},{...core.ACTION_CATALOG['rustdesk.connect'],challenge:'Challenge_0987654321zyxwvutsrq',challengeTtlSeconds:60}],raw={protocol:'rah-node-actions-v3',status:'ready',sessionId:s1,actions:rows};const catalog=core.sanitizeActionCatalog(raw,health.capabilities,s1);assert.deepEqual(catalog,{protocol:'rah-node-actions-v3',status:'ready',sessionId:s1,actions:['storage-summary.read','rustdesk.launch','rustdesk.connect']});assert.equal(core.sanitizeActionCatalog({...raw,sessionId:s2},health.capabilities,s1),null);assert.equal(core.actionChallengeFromCatalog(raw,health.capabilities,'storage-summary.read',s1),ch);assert.equal(core.actionChallengeFromCatalog(raw,health.capabilities,'storage-summary.read',s2),'');let devices=core.normalizeDeviceRegistry(null);devices=core.enrollDevice(devices,'lenovo-kali','192.168.1.44',health,catalog);let node=devices.find(d=>d.id==='lenovo-kali');assert.equal(node.enrolled,true);assert.equal(node.agentSessionId,s1);assert.deepEqual(node.advertisedActions,['storage-summary.read','rustdesk.launch','rustdesk.connect']);for(const key of['challenge','actionChallenge','token','peerId','password'])assert.equal(Object.hasOwn(node,key),false,key);devices=core.approveDeviceAction(devices,'lenovo-kali','storage-summary.read');node=devices.find(d=>d.id==='lenovo-kali');assert.deepEqual(node.approvedActions,['storage-summary.read']);const health2={...health,sessionId:s2};const catalog2={...catalog,sessionId:s2};devices=core.enrollDevice(devices,'lenovo-kali','192.168.1.44',health2,catalog2);node=devices.find(d=>d.id==='lenovo-kali');assert.equal(node.agentSessionId,s2);assert.deepEqual(node.approvedActions,[],'session change must clear local approvals on re-enrollment');assert.equal(core.canExecuteAction(node,'storage-summary.read'),false);devices=core.approveDeviceAction(devices,'lenovo-kali','storage-summary.read');node=devices.find(d=>d.id==='lenovo-kali');assert.equal(core.canExecuteAction(node,'storage-summary.read'),true);assert.equal(core.sanitizeActionCatalog(raw,node.capabilities,node.agentSessionId),null,'old session catalog must fail');const raw2={...raw,sessionId:s2};assert.ok(core.sanitizeActionCatalog(raw2,node.capabilities,node.agentSessionId));assert.equal(core.actionChallengeFromCatalog(raw2,node.capabilities,'storage-summary.read',node.agentSessionId),ch);const hostile=core.normalizeDeviceRegistry([{id:'x',label:'x',role:'x',platform:'x',kind:'desktop',enrolled:true,endpointIp:'192.168.1.5',agentSessionId:'bad session',capabilities:['remote-desktop'],advertisedActions:['rustdesk.connect'],approvedActions:['rustdesk.connect']}])[0];assert.equal(hostile.enrolled,false);assert.equal(hostile.agentSessionId,'');console.log('RAH Command Center core v1.2 session-bound enrollment tests passed');`;write('tests/rah-command-center-core.test.mjs',coreTest+'\n');

const v12=String.raw`import assert from'node:assert/strict';import fs from'node:fs';import{createRequire}from'node:module';const require=createRequire(import.meta.url),core=require('../rah-command-center-core.js'),cc=JSON.parse(fs.readFileSync('RAH-COMMAND-CENTER-VERSION.json','utf8')),raven=JSON.parse(fs.readFileSync('RAH-RAVEN-VERSION.json','utf8')),html=fs.readFileSync('RAH-COMMAND-CENTER-V1.2.html','utf8'),agent=fs.readFileSync('rah-node-agent.py','utf8');const stableCore={raven_vision:'0.6',raven_council:'0.3',agent_runner:'0.3',memory_sync:'0.2',mission_control:'2.9',project_focus:'2.4',raven_core:'1.12',raven_now:'2.17',raven_studio:'2.8'};const sections={manifest:()=>{assert.equal(cc.version,'1.2.0');assert.equal(cc.stage,'feature-candidate');assert.equal(cc.previous_stable_version,'1.1.0');assert.equal(cc.release_gate.status,'candidate');assert.equal(cc.release_gate.gate_version,'1.8.0-candidate');assert.equal(cc.node_agent.agent_version,'0.8.0');assert.equal(cc.node_agent.protocol,'rah-node-health-v2');assert.equal(cc.node_agent.actions_protocol,'rah-node-actions-v3');for(const[k,v]of Object.entries({session_bound_enrollment:true,agent_session_id_rotates_on_start:true,agent_session_id_node_memory_only:true,agent_session_id_secret:false,enrollment_session_id_persistence:true,enrollment_session_id_non_secret:true,enrollment_health_actions_session_match_required:true,enrollment_verified_catalog_sanitized_only:true,enrollment_pending_raw_action_challenge:false,enrollment_raw_action_challenge_persistence:false,agent_session_mismatch_blocks_action_challenge:true,agent_session_change_requires_reverification:true,agent_session_change_clears_approvals_on_reenroll:true,remote_control:false,device_commands:false,file_access:false,shell_access:false,credential_collection:false}))assert.equal(cc.features[k],v,k);assert.deepEqual(raven.release_gate.stable_components,stableCore)},ui:()=>{assert.match(html,/SESSION-BOUND ENROLLMENT/);assert.match(html,/sanitizeActionCatalog\(actionRes\.payload,health\.capabilities,health\.sessionId\)/);assert.match(html,/pendingEnrollment=\{id,ip,health,catalog\}/);assert.match(html,/core\.enrollDevice\(devices,pendingEnrollment\.id,pendingEnrollment\.ip,pendingEnrollment\.health,pendingEnrollment\.catalog\)/);assert.doesNotMatch(html,/pendingEnrollment\.catalog\.actions\.map/);assert.match(html,/d\.agentSessionId/);assert.match(html,/Re-check and re-enroll this node/);assert.doesNotMatch(html,/localStorage\.setItem\([^\n]*(?:challenge|token|peerId|password)/i)},agent:()=>{assert.match(agent,/AGENT_VERSION="0\.8\.0"/);assert.match(agent,/PROTOCOL="rah-node-health-v2"/);assert.match(agent,/ACTIONS_PROTOCOL="rah-node-actions-v3"/);assert.match(agent,/secrets\.token_urlsafe\(18\)/);assert.match(agent,/"sessionId":session/);assert.match(agent,/server_version="RAHNodeAgent\/0\.8"/);assert.doesNotMatch(agent,/os\.system|shell=True|"--password"/)},core:()=>{assert.equal(core.CC_VERSION,'1.2.0');assert.equal(core.NODE_AGENT_PROTOCOL,'rah-node-health-v2');assert.equal(core.NODE_ACTIONS_PROTOCOL,'rah-node-actions-v3');assert.deepEqual([...core.ACTION_IDS],['storage-summary.read','rustdesk.launch','rustdesk.connect'])}};const selected=process.env.RAH_CC_V12_SECTION||'';if(selected){assert.ok(Object.hasOwn(sections,selected));sections[selected]();console.log('PASS v1.2 '+selected)}else for(const[name,fn]of Object.entries(sections)){fn();console.log('PASS '+name)}`;write('tests/rah-command-center-v12.test.mjs',v12+'\n');

const pkg=String.raw`import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const m=JSON.parse(fs.readFileSync('RAH-COMMAND-CENTER-VERSION.json','utf8')),up=fs.readFileSync('UPDATE-RAH-COMMAND-CENTER.ps1','utf8'),launcher=fs.readFileSync('DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat','utf8');const files=['RAH-COMMAND-CENTER-V1.2.html','rah-command-center-core.js','DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat','rah-node-agent.py','START-RAH-NODE-AGENT.bat','START-RAH-NODE-AGENT.sh'];test('v1.2 candidate package',()=>{assert.equal(m.version,'1.2.0');assert.equal(m.stage,'feature-candidate');assert.equal(m.release_gate.status,'candidate');assert.deepEqual(m.package_files,files);assert.match(launcher,/RAH-COMMAND-CENTER-V1\.2\.html/);assert.doesNotMatch(launcher,/Invoke-WebRequest|curl\b|wget\b|https?:\/\//i)});test('updater exact allowlist and Stable guard',()=>{for(const f of files)assert.ok(up.includes(f),f);assert.match(up,/Assert-FixedPackageContract/);assert.match(up,/commit\.verification\.verified/);assert.match(up,/\$manifest\.stage -ne "stable"/);assert.match(up,/\$manifest\.release_gate\.status -ne "passed"/)});test('session binding adds no authority',()=>{assert.equal(m.features.session_bound_enrollment,true);assert.deepEqual(m.node_agent.action_allowlist,['storage-summary.read','rustdesk.launch','rustdesk.connect']);for(const k of['remote_control','device_commands','file_access','shell_access','credential_collection','generic_process_launch','generic_action_endpoint'])assert.equal(m.features[k],false,k)});`;write('tests/rah-command-center-packaging.test.mjs',pkg+'\n');

const py=String.raw`import importlib.util,json,threading,unittest,urllib.error,urllib.request
from pathlib import Path
ROOT=Path(__file__).resolve().parents[1];spec=importlib.util.spec_from_file_location('rah_node_agent',ROOT/'rah-node-agent.py');agent=importlib.util.module_from_spec(spec);spec.loader.exec_module(agent)
class T(unittest.TestCase):
 def setUp(self):
  self.token='token-abcdefghijklmnopqrstuvwxyz';self.session='SessionId_abcdefghijklmnop';self.launched=[];self.handoffs=[];self.server=agent.create_server('127.0.0.1',0,self.token,'Node','Role',['storage','remote-desktop'],{'rustdesk':'/fixed/rustdesk'},lambda p:self.launched.append(p) or True,lambda p,x:self.handoffs.append((p,x)) or True,session_id=self.session);self.port=self.server.server_address[1];self.th=threading.Thread(target=self.server.serve_forever,daemon=True);self.th.start()
 def tearDown(self):self.server.shutdown();self.server.server_close();self.th.join(timeout=2)
 def req(self,path,method='GET',data=None,challenge=None):
  h={'Origin':'null','Authorization':'Bearer '+self.token};
  if challenge:h[agent.ACTION_CHALLENGE_HEADER]=challenge
  if data is not None:h['Content-Type']='application/json'
  r=urllib.request.Request(f'http://127.0.0.1:{self.port}{path}',headers=h,method=method,data=data.encode() if isinstance(data,str) else data)
  try:
   with urllib.request.urlopen(r,timeout=2) as x:return x.status,json.loads(x.read() or b'{}')
  except urllib.error.HTTPError as e:return e.code,json.loads(e.read() or b'{}')
 def catalog(self):return self.req('/actions')[1]
 def test_health_actions_same_session(self):
  st,h=self.req('/health');self.assertEqual(st,200);a=self.catalog();self.assertEqual(h['protocol'],'rah-node-health-v2');self.assertEqual(a['protocol'],'rah-node-actions-v3');self.assertEqual(h['sessionId'],self.session);self.assertEqual(a['sessionId'],self.session);self.assertEqual(h['agentVersion'],'0.8.0')
 def test_restart_session_differs_by_default(self):
  s1=agent.create_server('127.0.0.1',0,'a'*32,capabilities=[]);s2=agent.create_server('127.0.0.1',0,'b'*32,capabilities=[])
  try:
   h1=s1.RequestHandlerClass;h2=s2.RequestHandlerClass;self.assertIsNot(h1,h2)
  finally:s1.server_close();s2.server_close()
  self.assertRegex(agent.sanitize_session_id('SessionId_abcdefghijklmnop'),r'^[A-Za-z0-9_-]{20,64}$')
 def test_challenge_still_single_use(self):
  c=self.catalog();row=next(x for x in c['actions'] if x['id']=='storage-summary.read');self.assertEqual(c['sessionId'],self.session);self.assertEqual(self.req('/storage',challenge=row['challenge'])[0],200);self.assertEqual(self.req('/storage',challenge=row['challenge'])[0],409)
 def test_handoff_boundaries_unchanged(self):
  c=self.catalog();row=next(x for x in c['actions'] if x['id']=='rustdesk.connect');self.assertEqual(self.req('/handoff/rustdesk','POST','{"peerId":"123456789","password":"x"}',row['challenge'])[0],400);c=self.catalog();row=next(x for x in c['actions'] if x['id']=='rustdesk.connect');self.assertEqual(self.req('/handoff/rustdesk','POST','{"peerId":"123456789"}',row['challenge'])[0],200);self.assertEqual(self.handoffs,[('/fixed/rustdesk','123456789')])
 def test_source_has_no_generic_authority(self):
  s=(ROOT/'rah-node-agent.py').read_text();self.assertNotIn('os.system',s);self.assertNotIn('shell=True',s);self.assertNotIn('"--password"',s);self.assertNotIn('/action/run',s)
if __name__=='__main__':unittest.main()`;write('tests/test_rah_node_agent.py',py+'\n');

// Permanent workflow
const wf=`name: Validate RAH Command Center v1.2\non:\n  pull_request:\n    paths: ['RAH-COMMAND-CENTER-V1.1.html','RAH-COMMAND-CENTER-V1.2.html','RAH-COMMAND-CENTER-VERSION.json','rah-command-center-core.js','rah-node-agent.py','START-RAH-NODE-AGENT.bat','START-RAH-NODE-AGENT.sh','UPDATE-RAH-COMMAND-CENTER.ps1','DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat','RAH-RAVEN-VERSION.json','tests/rah-command-center-v11.test.mjs','tests/rah-command-center-v12.test.mjs','tests/rah-command-center-core.test.mjs','tests/rah-command-center-packaging.test.mjs','tests/test_rah_node_agent.py','.github/workflows/validate-rah-command-center.yml']\n  push:\n    branches: [main]\n    paths: ['RAH-COMMAND-CENTER-V1.1.html','RAH-COMMAND-CENTER-V1.2.html','RAH-COMMAND-CENTER-VERSION.json','rah-command-center-core.js','rah-node-agent.py','START-RAH-NODE-AGENT.bat','START-RAH-NODE-AGENT.sh','UPDATE-RAH-COMMAND-CENTER.ps1','DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat','RAH-RAVEN-VERSION.json','tests/rah-command-center-v11.test.mjs','tests/rah-command-center-v12.test.mjs','tests/rah-command-center-core.test.mjs','tests/rah-command-center-packaging.test.mjs','tests/test_rah_node_agent.py','.github/workflows/validate-rah-command-center.yml']\npermissions: {contents: read}\njobs:\n  validate:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: {node-version: '22'}\n      - uses: actions/setup-python@v5\n        with: {python-version: '3.12'}\n      - name: v1.2 core\n        run: node --test tests/rah-command-center-core.test.mjs\n      - name: Preserve v1.1 artifact\n        run: node --test tests/rah-command-center-v11.test.mjs\n      - name: v1.2 manifest\n        run: RAH_CC_V12_SECTION=manifest node --test tests/rah-command-center-v12.test.mjs\n      - name: v1.2 UI enrollment\n        run: RAH_CC_V12_SECTION=ui node --test tests/rah-command-center-v12.test.mjs\n      - name: v1.2 agent session\n        run: RAH_CC_V12_SECTION=agent node --test tests/rah-command-center-v12.test.mjs\n      - name: v1.2 typed core\n        run: RAH_CC_V12_SECTION=core node --test tests/rah-command-center-v12.test.mjs\n      - name: v1.2 packaging\n        run: node --test tests/rah-command-center-packaging.test.mjs\n      - name: Node Agent v0.8 HTTP session gate\n        run: python -m unittest tests/test_rah_node_agent.py -v\n      - name: Parse updater\n        shell: pwsh\n        run: |\n          $errors=$null\n          [System.Management.Automation.Language.Parser]::ParseFile('UPDATE-RAH-COMMAND-CENTER.ps1',[ref]$null,[ref]$errors)|Out-Null\n          if($errors.Count -gt 0){$errors|Format-List;exit 1}\n      - name: Raven release gate\n        run: node --test tests/raven-release-gate.test.mjs\n      - name: Project Registry stage-aware gate\n        run: node --test tests/raven-project-registry-v0.1.test.mjs\n`;write('.github/workflows/validate-rah-command-center.yml',wf);
console.log('CC v1.2 Candidate generated; v1.1 HTML blob:',blob('RAH-COMMAND-CENTER-V1.1.html'));
