import fs from'node:fs';import assert from'node:assert/strict';const ccPath='RAH-COMMAND-CENTER-VERSION.json',ravenPath='RAH-RAVEN-VERSION.json',cc=JSON.parse(fs.readFileSync(ccPath,'utf8')),raven=JSON.parse(fs.readFileSync(ravenPath,'utf8')),stableBefore=JSON.stringify(raven.release_gate?.stable_components||{});assert.equal(cc.version,'1.3.0');assert.equal(cc.stage,'feature-candidate');assert.equal(cc.release_gate.status,'candidate');assert.equal(cc.node_agent.agent_version,'0.9.0');assert.equal(cc.node_agent.auth.protocol,'rah-node-auth-v1');assert.deepEqual(cc.node_agent.action_allowlist,['storage-summary.read','rustdesk.launch','rustdesk.connect']);for(const k of['remote_control','device_commands','file_access','shell_access','credential_collection','generic_process_launch','generic_action_endpoint'])assert.equal(cc.features[k],false,k);cc.stage='stable';cc.stable_since='2026-08-15';cc.release_gate={...cc.release_gate,status:'passed',gate_version:'1.9.0',runtime_files_frozen:true,change_policy:'bugfix-only-until-explicit-reopen'};cc.development_paused=true;cc.change_policy='bugfix-only-until-explicit-reopen';cc.node_agent.action_catalog.bearer_compromise_protection=true;assert.equal(cc.features.token_network_transport,false);assert.equal(cc.features.bearer_authorization_transport,false);assert.equal(cc.features.auth_proof_fallback,false);assert.equal(cc.node_agent.auth.token_transport,false);assert.equal(cc.node_agent.auth.bearer_fallback,false);fs.writeFileSync(ccPath,JSON.stringify(cc,null,2)+'\n');assert.equal(raven.product,'RAH Raven');assert.equal(raven.version,'2.0.32');const start=raven.summary.indexOf('RAH Raven Command Center v1.2.0 is stable.'),end=raven.summary.indexOf(' Raven Chronicle v1.7.1 is stable');assert.ok(start>=0&&end>start,'Expected v1.2 Command Center summary segment not found');const text='RAH Raven Command Center v1.3.0 is stable. Command Center v1.3 preserves the exact fixed action allowlist storage-summary.read, rustdesk.launch and rustdesk.connect, the v1.1 single-use action challenge, and v1.2 session-bound enrollment, while replacing bearer-token transport over LAN with token-proof authentication. A fresh Node Agent token is still required for every node check and action, but it remains local in Command Center. Node Agent v0.9 exposes only fixed GET /auth/challenge for authentication setup; the challenge contains a short-lived non-secret nonce bound to the current agent session. Command Center uses browser WebCrypto HMAC-SHA256 over the RAH-AUTH-V1 canonical transcript binding sessionId, nonce, HTTP method, exact fixed path and SHA256 of the exact request body. LAN traffic carries X-RAH-Auth-Nonce and X-RAH-Auth-Proof instead of the token; Authorization/Bearer transport is rejected and there is no bearer fallback. Protected actions still additionally require the enrolled session, correct capability, advertised action, explicit local per-device approval and a fresh single-use action challenge. The auth nonce and action challenge are memory-only and single-use; token, proof, nonce, action challenge, RustDesk peer ID and password are not persisted. Passwords are not accepted or transported. User-supplied executable paths, arbitrary arguments, server/key/URL overrides, installation, generic process or action endpoints, shell access, file access, device commands, background polling, discovery and native Raven remote control remain disabled. RustDesk handoff still claims only handoff-started, not a successful remote session.';raven.summary=raven.summary.slice(0,start)+text+raven.summary.slice(end);const idx=raven.files.indexOf('RAH-COMMAND-CENTER-V1.2.html');assert.ok(idx>=0,'v1.2 file baseline missing');if(!raven.files.includes('RAH-COMMAND-CENTER-V1.3.html'))raven.files.splice(idx+1,0,'RAH-COMMAND-CENTER-V1.3.html');Object.assign(raven.privacy,{command_center_version_synced:true,command_center_stable:true,command_center_runtime_frozen:true,command_center_node_agent_version_synced:true,command_center_action_execution_requires_bearer_token:false,command_center_rustdesk_launch_requires_bearer_token:false,command_center_rustdesk_handoff_requires_bearer_token:false,command_center_node_storage_read_requires_bearer_token:false,command_center_action_challenge_bearer_compromise_protection:true,command_center_token_proof_authentication:true,command_center_token_network_transport:false,command_center_bearer_authorization_transport:false,command_center_auth_protocol_v1:true,command_center_auth_challenge_path_fixed:true,command_center_auth_nonce_ttl_30_seconds:true,command_center_auth_nonce_single_use:true,command_center_auth_nonce_client_bound:true,command_center_auth_proof_hmac_sha256:true,command_center_auth_proof_binds_session:true,command_center_auth_proof_binds_method:true,command_center_auth_proof_binds_fixed_path:true,command_center_auth_proof_binds_body_sha256:true,command_center_auth_proof_webcrypto_required:true,command_center_auth_proof_fallback:false,command_center_auth_proof_persistence:false,command_center_auth_nonce_persistence:false,command_center_token_local_only_transient:true,command_center_fresh_node_agent_token_required:true,command_center_generic_process_launch:false,command_center_generic_action_endpoint:false,command_center_node_native_remote_control_endpoints:false,command_center_device_background_polling:false,command_center_network_discovery:false,command_center_remote_control:false,command_center_device_commands:false,command_center_file_access:false,command_center_shell_access:false,command_center_credential_collection:false});assert.equal(JSON.stringify(raven.release_gate?.stable_components||{}),stableBefore,'Stable Raven core components changed');assert.equal(raven.files.filter(x=>x==='RAH-COMMAND-CENTER-V1.3.html').length,1);assert.match(raven.summary,/Command Center v1\.3 preserves the exact fixed action allowlist/);assert.equal(raven.privacy.command_center_token_network_transport,false);assert.equal(raven.privacy.command_center_bearer_authorization_transport,false);assert.equal(raven.privacy.command_center_remote_control,false);fs.writeFileSync(ravenPath,JSON.stringify(raven,null,2)+'\n');console.log('RAH CC v1.3 Stable metadata + Raven master sync generated without authority expansion.');