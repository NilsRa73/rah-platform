import assert from'node:assert/strict';import fs from'node:fs';import crypto from'node:crypto';
const path='RAH-COMMAND-CENTER-V1.0.html',html=fs.readFileSync(path,'utf8'),buf=fs.readFileSync(path),sha=crypto.createHash('sha1').update(Buffer.concat([Buffer.from('blob '+buf.length+'\0'),buf])).digest('hex');
assert.equal(sha,'876d4bdc97b27924270830b807445b89422544e9','v1.0 Stable HTML moved');assert.match(html,/CC v1\.0\.0/);assert.match(html,/REMOTE DESKTOP HANDOFF/);assert.match(html,/rustdesk\.connect/);assert.match(html,/Target ID and token are transient/);assert.doesNotMatch(html,/id="handoffPassword"|--password|generic action endpoint/i);console.log('RAH Command Center v1.0 historical artifact preserved');
