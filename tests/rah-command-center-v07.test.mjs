import assert from'node:assert/strict';import fs from'node:fs';const raven=JSON.parse(fs.readFileSync('RAH-RAVEN-VERSION.json','utf8')),current=JSON.parse(fs.readFileSync('RAH-COMMAND-CENTER-VERSION.json','utf8')),html=fs.readFileSync('RAH-COMMAND-CENTER-V0.7.html','utf8');const expected={raven_vision:'0.6',raven_council:'0.3',agent_runner:'0.3',memory_sync:'0.2',mission_control:'2.9',project_focus:'2.4',raven_core:'1.12',raven_now:'2.17',raven_studio:'2.8'};
assert.equal(current.version,'0.8.0');assert.equal(current.previous_stable_version,'0.7.0');assert.deepEqual(raven.release_gate.stable_components,expected);assert.equal(raven.files.includes('RAH-COMMAND-CENTER-V0.7.html'),true);assert.match(html,/<title>RAH Raven Command Center v0\.7\.0<\/title>/);assert.match(html,/EXPLICIT DEVICE ACTION/);assert.match(html,/Read system-volume storage/);assert.match(html,/GET only/);assert.match(html,/fixed volume/);assert.match(html,/no filenames/i);assert.match(html,/\/storage/);assert.match(html,/Authorization:'Bearer '\+token/);assert.doesNotMatch(html,/[?&]token=/i);assert.doesNotMatch(html,/setInterval\s*\(/);assert.doesNotMatch(html,/\/agent\/run|method:'POST'|method:"POST"/);assert.doesNotMatch(html,/pathInput|requestedPath|filePath/i);console.log('RAH Command Center v0.7 historical explicit storage-read artifact preserved after v0.8 master sync');
