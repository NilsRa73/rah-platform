import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const manifest=JSON.parse(fs.readFileSync('RAH-COMMAND-CENTER-VERSION.json','utf8')),updater=fs.readFileSync('UPDATE-RAH-COMMAND-CENTER.ps1','utf8'),ravenUpdater=fs.readFileSync('UPDATE-RAH-RAVEN.ps1','utf8'),launcher=fs.readFileSync('DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat','utf8'),nodeBat=fs.readFileSync('START-RAH-NODE-AGENT.bat','utf8'),nodeSh=fs.readFileSync('START-RAH-NODE-AGENT.sh','utf8');const packageFiles=['RAH-COMMAND-CENTER-V0.6.html','rah-command-center-core.js','DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat','rah-node-agent.py','START-RAH-NODE-AGENT.bat','START-RAH-NODE-AGENT.sh'];
test('v0.6 candidate keeps normal launch offline-first',()=>{assert.equal(manifest.version,'0.6.0');assert.equal(manifest.stage,'feature-candidate');assert.equal(manifest.release_gate.status,'candidate');assert.deepEqual(manifest.package_files,packageFiles);assert.match(launcher,/RAH-COMMAND-CENTER-V0\.6\.html/);assert.doesNotMatch(launcher,/Invoke-WebRequest|powershell|pwsh|python|curl\b|wget\b|https?:\/\//i)});test('verified updater has exact v0.6 allowlist and Stable guard',()=>{for(const file of packageFiles)assert.ok(updater.includes(file),file);assert.match(updater,/Assert-FixedPackageContract/);assert.match(updater,/commit\.verification\.verified/);assert.match(updater,/\$manifest\.stage -ne "stable"/);assert.match(updater,/\$manifest\.release_gate\.status -ne "passed"/);assert.doesNotMatch(updater,/raw\.githubusercontent\.com\/NilsRa73\/rah-platform\/main/)});test('Node Agent capability flags remain explicit',()=>{assert.equal(manifest.node_agent.capability_source,'explicit-cli-flags-only');assert.match(nodeBat,/--allow-lan %\*/);assert.match(nodeSh,/--allow-lan "\$@"/);assert.doesNotMatch(updater,/Start-Process[^\n]*rah-node-agent|&[^\n]*rah-node-agent/i);assert.doesNotMatch(launcher,/rah-node-agent|START-RAH-NODE-AGENT/i)});test('Raven updater does not bootstrap remote updater code',()=>{const block=ravenUpdater.match(/function Sync-CommandCenterPackage \{[\s\S]*?\n\}/)?.[0]||'';assert.match(block,/-File \$ccUpdater -NoStart/);assert.doesNotMatch(block,/Invoke-WebRequest|raw\.githubusercontent\.com|\.rah-download/i)});test('all action permissions remain disabled',()=>{assert.deepEqual(manifest.node_agent.permissions,{healthRead:true,capabilityRead:true,commands:false,files:false,shell:false,remoteControl:false});for(const k of ['network_discovery','device_background_polling','remote_control','device_commands','file_access','shell_access','credential_collection'])assert.equal(manifest.features[k],false,k)});
