import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const manifest=JSON.parse(fs.readFileSync('RAH-COMMAND-CENTER-VERSION.json','utf8')),updater=fs.readFileSync('UPDATE-RAH-COMMAND-CENTER.ps1','utf8'),ravenUpdater=fs.readFileSync('UPDATE-RAH-RAVEN.ps1','utf8'),launcher=fs.readFileSync('DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat','utf8'),nodeBat=fs.readFileSync('START-RAH-NODE-AGENT.bat','utf8'),nodeSh=fs.readFileSync('START-RAH-NODE-AGENT.sh','utf8');
const packageFiles=['RAH-COMMAND-CENTER-V0.5.html','rah-command-center-core.js','DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat','rah-node-agent.py','START-RAH-NODE-AGENT.bat','START-RAH-NODE-AGENT.sh'];
test('v0.5 candidate keeps normal launch offline-first',()=>{assert.equal(manifest.version,'0.5.0');assert.equal(manifest.stage,'feature-candidate');assert.equal(manifest.previous_stable_version,'0.4.1');assert.deepEqual(manifest.package_files,packageFiles);assert.equal(manifest.features.offline_first_one_click_launcher,true);assert.equal(manifest.features.launcher_network_requests,false);assert.equal(manifest.features.automatic_update_on_launch,false);assert.match(launcher,/RAH-COMMAND-CENTER-V0\.5\.html/);assert.match(launcher,/start "" "%CC_PAGE%"/);assert.doesNotMatch(launcher,/Invoke-WebRequest|powershell|pwsh|python|curl\b|wget\b|https?:\/\//i)});
test('verified manual updater has exact v0.5 allowlist',()=>{assert.equal(manifest.features.manual_updater_verified_commit_resolution,true);assert.equal(manifest.features.manual_updater_immutable_commit_downloads,true);assert.equal(manifest.features.manual_updater_stable_manifest_required,true);assert.equal(manifest.features.manual_updater_fixed_package_allowlist,true);for(const file of packageFiles)assert.ok(updater.includes(file),file);assert.match(updater,/Assert-FixedPackageContract/);assert.match(updater,/commit\.verification\.verified/);assert.match(updater,/raw\.githubusercontent\.com\/\$RepoOwner\/\$RepoName\/\$ResolvedCommit/);assert.match(updater,/\$manifest\.stage -ne "stable"/);assert.match(updater,/\$manifest\.release_gate\.status -ne "passed"/);assert.doesNotMatch(updater,/raw\.githubusercontent\.com\/NilsRa73\/rah-platform\/main/)});
test('Node Agent is packaged but never automatically started',()=>{assert.equal(manifest.node_agent.automatic_start,false);assert.equal(manifest.node_agent.automatic_discovery,false);assert.equal(manifest.node_agent.commands,false);assert.match(nodeBat,/rah-node-agent\.py" --allow-lan/);assert.match(nodeSh,/python3 \.\/rah-node-agent\.py --allow-lan/);assert.doesNotMatch(updater,/Start-Process[^\n]*rah-node-agent|&[^\n]*rah-node-agent/i);assert.doesNotMatch(launcher,/rah-node-agent|START-RAH-NODE-AGENT/i)});
test('Raven updater still never downloads executable CC updater code',()=>{assert.equal(manifest.features.raven_updater_remote_cc_bootstrap,false);const block=ravenUpdater.match(/function Sync-CommandCenterPackage \{[\s\S]*?\n\}/)?.[0]||'';assert.match(block,/-File \$ccUpdater -NoStart/);assert.doesNotMatch(block,/Invoke-WebRequest|raw\.githubusercontent\.com|\.rah-download/i)});
test('remote control and commands remain disabled',()=>{assert.equal(manifest.features.network_discovery,false);assert.equal(manifest.features.device_background_polling,false);assert.equal(manifest.features.remote_control,false);assert.equal(manifest.features.device_commands,false);assert.equal(manifest.features.credential_collection,false)});
