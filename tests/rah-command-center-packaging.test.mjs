import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const manifest=JSON.parse(fs.readFileSync('RAH-COMMAND-CENTER-VERSION.json','utf8')),updater=fs.readFileSync('UPDATE-RAH-COMMAND-CENTER.ps1','utf8'),ravenUpdater=fs.readFileSync('UPDATE-RAH-RAVEN.ps1','utf8'),launcher=fs.readFileSync('DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat','utf8'),nodeBat=fs.readFileSync('START-RAH-NODE-AGENT.bat','utf8'),nodeSh=fs.readFileSync('START-RAH-NODE-AGENT.sh','utf8');const packageFiles=['RAH-COMMAND-CENTER-V0.9.html','rah-command-center-core.js','DOBBELTKLIKK-HER-START-RAH-COMMAND-CENTER.bat','rah-node-agent.py','START-RAH-NODE-AGENT.bat','START-RAH-NODE-AGENT.sh'];
test('v0.9 Stable keeps normal launch offline-first',()=>{assert.equal(manifest.version,'0.9.0');assert.equal(manifest.stage,'stable');assert.equal(manifest.stable_since,'2026-08-15');assert.equal(manifest.release_gate.status,'passed');assert.equal(manifest.release_gate.gate_version,'1.5.0');assert.equal(manifest.release_gate.runtime_files_frozen,true);assert.equal(manifest.development_paused,true);assert.equal(manifest.change_policy,'bugfix-only-until-explicit-reopen');assert.deepEqual(manifest.package_files,packageFiles);assert.match(launcher,/RAH-COMMAND-CENTER-V0\.9\.html/);assert.match(launcher,/RAH RAVEN COMMAND CENTER v0\.9\.0/);assert.doesNotMatch(launcher,/Invoke-WebRequest|powershell|pwsh|python|curl\b|wget\b|https?:\/\//i)});
test('verified updater has exact v0.9 allowlist and Stable guard',()=>{for(const file of packageFiles)assert.ok(updater.includes(file),file);assert.match(updater,/Assert-FixedPackageContract/);assert.match(updater,/commit\.verification\.verified/);assert.match(updater,/\$manifest\.stage -ne "stable"/);assert.match(updater,/\$manifest\.release_gate\.status -ne "passed"/);assert.doesNotMatch(updater,/raw\.githubusercontent\.com\/NilsRa73\/rah-platform\/main/)});
test('Node Agent remains explicit and manual',()=>{assert.equal(manifest.node_agent.capability_source,'explicit-cli-flags-only');assert.equal(manifest.node_agent.action_catalog.approval_mode,'command-center-local');assert.match(nodeBat,/--allow-lan %\*/);assert.match(nodeSh,/--allow-lan "\$@"/);assert.match(nodeBat,/\/launch\/rustdesk/);assert.match(nodeSh,/\/launch\/rustdesk/);assert.doesNotMatch(updater,/Start-Process[^\n]*rah-node-agent|&[^\n]*rah-node-agent/i);assert.doesNotMatch(launcher,/rah-node-agent|START-RAH-NODE-AGENT/i)});
test('external app launcher contract is fixed and explicit',()=>{assert.equal(manifest.features.external_app_launchers,true);assert.equal(manifest.features.external_app_allowlist,true);assert.deepEqual(manifest.features.external_app_ids,['rustdesk']);assert.equal(manifest.features.rustdesk_launch_action,true);assert.equal(manifest.features.rustdesk_launch_requires_remote_desktop_capability,true);assert.equal(manifest.features.rustdesk_launch_requires_local_approval,true);assert.equal(manifest.features.rustdesk_launch_requires_bearer_token,true);assert.equal(manifest.features.rustdesk_launch_post_only,true);assert.equal(manifest.features.rustdesk_launch_no_request_body,true);assert.equal(manifest.features.rustdesk_launch_user_supplied_path,false);assert.equal(manifest.features.rustdesk_launch_user_supplied_arguments,false);assert.equal(manifest.features.rustdesk_installation,false);assert.equal(manifest.features.generic_process_launch,false);assert.deepEqual(manifest.node_agent.action_allowlist,['storage-summary.read','rustdesk.launch']);assert.deepEqual(manifest.node_agent.external_app_launchers.allowlist,['rustdesk']);assert.equal(manifest.node_agent.external_app_launchers.generic_process_launch,false);assert.equal(manifest.node_agent.rustdesk_launch.method,'POST');assert.equal(manifest.node_agent.rustdesk_launch.path,'/launch/rustdesk');assert.equal(manifest.node_agent.rustdesk_launch.request_body,false)});
test('storage read remains narrow and non-persistent',()=>{assert.equal(manifest.features.node_agent_storage_summary_read,true);assert.equal(manifest.features.node_agent_storage_read_get_only,true);assert.equal(manifest.features.storage_summary_fixed_system_volume,true);assert.equal(manifest.features.storage_summary_arbitrary_path,false);assert.equal(manifest.features.storage_summary_file_listing,false);assert.equal(manifest.features.storage_summary_result_persistence,false);assert.equal(manifest.node_agent.storage_read.requires_capability,'storage');assert.equal(manifest.node_agent.storage_read.requires_action,'storage-summary.read');assert.equal(manifest.node_agent.storage_read.method,'GET')});
test('Raven updater does not bootstrap remote updater code',()=>{const block=ravenUpdater.match(/function Sync-CommandCenterPackage \{[\s\S]*?\n\}/)?.[0]||'';assert.match(block,/-File \$ccUpdater -NoStart/);assert.doesNotMatch(block,/Invoke-WebRequest|raw\.githubusercontent\.com|\.rah-download/i)});
test('dangerous generic permissions remain disabled',()=>{for(const k of['network_discovery','device_background_polling','remote_control','device_commands','file_access','shell_access','credential_collection','generic_process_launch','rustdesk_installation'])assert.equal(manifest.features[k],false,k);assert.equal(manifest.node_agent.permissions.commands,false);assert.equal(manifest.node_agent.permissions.files,false);assert.equal(manifest.node_agent.permissions.shell,false);assert.equal(manifest.node_agent.permissions.remoteControl,false);assert.equal(manifest.node_agent.permissions.externalAppLaunch,'requires-remote-desktop-capability-and-cc-approval')});
