import assert from 'node:assert/strict';
import { createRequire } from 'node:module';
const require=createRequire(import.meta.url);const core=require('../rah-command-center-core.js');
assert.equal(core.CC_VERSION,'0.5.0');assert.equal(core.RAVEN_VERSION,'2.0.32');assert.equal(core.DEVICE_STORAGE_KEY,'rah.cc.devices.v1');assert.equal(core.NODE_AGENT_PORT,18766);assert.equal(core.NODE_AGENT_PROTOCOL,'rah-node-health-v1');
const fallback=core.buildCoreSnapshot(null);assert.equal(fallback.stableCount,9);assert.equal(fallback.totalCount,9);
const live=core.buildCoreSnapshot({version:'2.0.32',release_gate:{stage:'temporary-stable',stable_components:{raven_vision:'0.6',raven_council:'0.3',agent_runner:'0.3',memory_sync:'0.2',mission_control:'2.9',project_focus:'2.4',raven_core:'1.12',raven_now:'2.17',raven_studio:'2.8'}}},'manifest');assert.equal(live.stableCount,9);
for(const ip of ['127.0.0.1','10.1.2.3','172.16.0.1','172.31.255.254','192.168.50.10'])assert.equal(core.isAllowedNodeIpv4(ip),true,ip);
for(const ip of ['172.32.0.1','169.254.1.1','8.8.8.8','localhost','192.168.1.2.example.com'])assert.equal(core.isAllowedNodeIpv4(ip),false,ip);
assert.equal(core.nodeHealthUrl('192.168.1.25'),'http://192.168.1.25:18766/health');assert.equal(core.nodeHealthUrl('8.8.8.8'),'');assert.equal(core.nodeHealthUrl('http://192.168.1.25'),'');
const health=core.sanitizeNodeHealth({protocol:'rah-node-health-v1',status:'ready',agentVersion:'0.1.0',hostname:'LENOVO',platform:'Linux',platformRelease:'6.8',machine:'x86_64'});assert.equal(health.hostname,'LENOVO');assert.equal(core.sanitizeNodeHealth({protocol:'wrong',status:'ready'}),null);
const defaults=core.normalizeDeviceRegistry(null);assert.equal(defaults.length,4);assert.equal(defaults.every(d=>!d.remoteControlEnabled&&!d.commandsEnabled),true);
const hostile=core.normalizeDeviceRegistry([{id:'../My PC',label:' Test\u0000 PC ',role:'Node',platform:'Windows',kind:'desktop',status:'online',endpointIp:'8.8.8.8',enrolled:true,remoteControlEnabled:true,commandsEnabled:true,token:'secret'}])[0];assert.equal(hostile.id,'my-pc');assert.equal(hostile.status,'unverified');assert.equal(hostile.enrolled,false);assert.equal(hostile.endpointIp,'');assert.equal(hostile.remoteControlEnabled,false);assert.equal(hostile.commandsEnabled,false);assert.equal(Object.hasOwn(hostile,'token'),false);
const enrolled=core.enrollDevice(defaults,'lenovo-kali','192.168.1.44',health),lenovo=enrolled.find(d=>d.id==='lenovo-kali');assert.equal(lenovo.enrolled,true);assert.equal(lenovo.endpointIp,'192.168.1.44');assert.equal(lenovo.agentHostname,'LENOVO');assert.equal(lenovo.remoteControlEnabled,false);assert.equal(lenovo.commandsEnabled,false);assert.equal(Object.hasOwn(lenovo,'token'),false);
assert.equal(core.enrollDevice(defaults,'lenovo-kali','8.8.8.8',health).find(d=>d.id==='lenovo-kali').enrolled,false);assert.equal(core.forgetEnrollment(enrolled,'lenovo-kali').find(d=>d.id==='lenovo-kali').endpointIp,'');
const snap=core.buildDeviceSnapshot(core.markThisDevice(enrolled,'main-pc'));assert.equal(snap.totalCount,4);assert.equal(snap.enrolledCount,1);assert.equal(snap.thisDeviceCount,1);assert.equal(snap.remoteControlCount,0);assert.equal(snap.commandCount,0);
assert.equal(core.PACKAGE_COMPONENTS.length,6);assert.equal(core.isCanonicalBridgeUrl('http://127.0.0.1:18765'),true);assert.equal(core.isCanonicalBridgeUrl('http://localhost:18765'),false);assert.equal(core.bridgeHealthUrl('http://evil.example'),'http://127.0.0.1:18765/health');
console.log('RAH Command Center core v0.5 enrollment tests passed');
