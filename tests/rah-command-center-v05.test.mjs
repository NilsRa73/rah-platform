import assert from 'node:assert/strict';import fs from 'node:fs';
const html=fs.readFileSync('RAH-COMMAND-CENTER-V0.5.html','utf8');
assert.match(html,/RAH Raven Command Center v0\.5\.0/);assert.match(html,/EXPLICIT DEVICE ENROLLMENT/);assert.match(html,/Check node/);assert.match(html,/Enroll verified node/);assert.match(html,/memory-only token/i);assert.match(html,/18766/);assert.match(html,/Authorization:'Bearer '\+token/);assert.match(html,/credentials:'omit'/);assert.match(html,/nodeToken'\)\.value=''/);assert.match(html,/function saveDevices\(\)\{localStorage\.setItem\(core\.DEVICE_STORAGE_KEY,JSON\.stringify\(devices\)\)\}/);assert.doesNotMatch(html,/localStorage\.setItem\([^)]*nodeToken/i);assert.doesNotMatch(html,/sessionStorage\.setItem\([^)]*nodeToken/i);assert.doesNotMatch(html,/[?&]token=/i);assert.doesNotMatch(html,/setInterval\s*\(/);assert.doesNotMatch(html,/WebSocket\s*\(|RTCPeerConnection|navigator\.bluetooth|navigator\.usb|navigator\.serial/i);assert.doesNotMatch(html,/\/agent\/run|\/command|\/shell/i);
console.log('RAH Command Center v0.5 historical enrollment artifact remains frozen');
