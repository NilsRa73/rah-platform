import assert from 'node:assert/strict';import fs from 'node:fs';import {createRequire} from 'node:module';const require=createRequire(import.meta.url),core=require('../rah-command-center-core.js'),raven=JSON.parse(fs.readFileSync('RAH-RAVEN-VERSION.json','utf8')),cc=JSON.parse(fs.readFileSync('RAH-COMMAND-CENTER-VERSION.json','utf8')),html=fs.readFileSync('RAH-COMMAND-CENTER-V0.5.html','utf8'),agent=fs.readFileSync('rah-node-agent.py','utf8');
const expected={raven_vision:'0.6',raven_council:'0.3',agent_runner:'0.3',memory_sync:'0.2',mission_control:'2.9',project_focus:'2.4',raven_core:'1.12',raven_now:'2.17',raven_studio:'2.8'};
assert.equal(cc.version,'0.5.0');assert.equal(cc.stage,'feature-candidate');assert.equal(cc.previous_stable_version,'0.4.1');assert.equal(cc.entry,'RAH-COMMAND-CENTER-V0.5.html');assert.equal(cc.runtime_feature_change,true);assert.equal(cc.development_reopened,true);assert.equal(cc.release_gate.status,'candidate');assert.equal(cc.release_gate.stable_raven_runtime_frozen,true);
for(const [k,v] of Object.entries({explicit_device_enrollment:true,node_agent_read_only_health:true,node_agent_fixed_port:18766,node_agent_default_loopback_bind:true,node_agent_lan_bind_requires_explicit_flag:true,node_agent_token_memory_only:true,node_agent_command_endpoints:false,node_agent_file_endpoints:false,node_agent_shell_endpoints:false,enrollment_private_ipv4_only:true,enrollment_token_local_storage:false,enrollment_token_url_transport:false,device_health_check_explicit_only:true,device_background_polling:false,network_discovery:false,remote_control:false,device_commands:false,credential_collection:false}))assert.equal(cc.features[k],v,k);
assert.deepEqual(raven.release_gate.stable_components,expected);assert.match(raven.summary,/Command Center v0\.4\.1/);assert.equal(raven.privacy.command_center_stable,true);assert.equal(raven.privacy.command_center_runtime_frozen,true);
assert.match(html,/DEVICE ENROLLMENT/);assert.match(html,/Check node/);assert.match(html,/Enroll verified node/);assert.match(html,/memory-only token/i);assert.match(html,/18766/);assert.match(html,/Authorization:'Bearer '\+token/);assert.match(html,/credentials:'omit'/);assert.match(html,/clearTimeout\(timer\)/);assert.match(html,/nodeToken'\)\.value=''/);assert.doesNotMatch(html,/localStorage\.setItem\([^\n]*token/i);assert.doesNotMatch(html,/sessionStorage\.setItem\([^\n]*token/i);assert.doesNotMatch(html,/[?&]token=/i);assert.doesNotMatch(html,/setInterval\s*\(/);assert.doesNotMatch(html,/WebSocket\s*\(|RTCPeerConnection|navigator\.bluetooth|navigator\.usb|navigator\.serial/i);assert.doesNotMatch(html,/\/agent\/run|\/command|\/shell/i);
assert.match(agent,/PORT=18766|PORT = 18766/);assert.match(agent,/PROTOCOL="rah-node-health-v1"|PROTOCOL = "rah-node-health-v1"/);assert.match(agent,/host="0\.0\.0\.0" if args\.allow_lan else "127\.0\.0\.1"/);assert.match(agent,/secrets\.token_urlsafe\(32\)/);assert.match(agent,/hmac\.compare_digest/);assert.match(agent,/self\.path!="\/health"|self\.path != "\/health"/);assert.match(agent,/def do_POST/);assert.doesNotMatch(agent,/subprocess|os\.system|Popen|eval\(|exec\(/);
const snap=core.buildDeviceSnapshot(null);assert.equal(snap.remoteControlCount,0);assert.equal(snap.commandCount,0);console.log('RAH Command Center v0.5 explicit enrollment candidate gate passed');
