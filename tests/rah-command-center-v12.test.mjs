import assert from'node:assert/strict';import fs from'node:fs';const html=fs.readFileSync('RAH-COMMAND-CENTER-V1.2.html','utf8');assert.match(html,/RAH Raven Command Center v1\.2\.0/);assert.match(html,/SESSION-BOUND ENROLLMENT/);assert.match(html,/Authorization:'Bearer '\+token/);assert.match(html,/sanitizeActionCatalog\(actionRes\.payload,health\.capabilities,health\.sessionId\)/);assert.match(html,/pendingEnrollment=\{id,ip,health,catalog\}/);assert.match(html,/d\.agentSessionId/);assert.doesNotMatch(html,/localStorage\.setItem\([^\n]*(?:challenge|token|peerId|password)/i);console.log('RAH Command Center v1.2 historical session-bound artifact: PASS');